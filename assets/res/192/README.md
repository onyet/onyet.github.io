# assets/res/192/

Contains 192px icons (often used for Android/web manifest). Keep these optimized.
`index.html` added to prevent directory listing.

Berisi ikon 192px (sering digunakan di Android/web manifest). Pastikan tetap teroptimasi.
