# assets/

Folder ini berisi aset statis untuk situs (gambar, fonts, libs, dll).
Untuk mencegah listing direktori, file `index.html` kosong sudah ditambahkan di setiap subfolder.

This folder contains static assets for the site (images, fonts, libs, etc.).
An empty `index.html` file has been added to each subfolder to prevent directory listing.
