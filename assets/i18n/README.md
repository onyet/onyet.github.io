# assets/i18n/

Contains translation JSON files used to localize the site. Edit `translations.json` to update strings.
An `index.html` file is present to prevent directory listing.

Berisi file JSON terjemahan untuk lokalisasi situs. Sunting `translations.json` untuk memperbarui string.
Terdapat `index.html` untuk mencegah listing direktori.
