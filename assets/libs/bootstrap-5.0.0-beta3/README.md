# assets/libs/bootstrap-5.0.0-beta3/

Bootstrap distribution used for layout and components. Avoid modifying library files; prefer updating upstream package.
`index.html` added to prevent directory listing.

Distribusi Bootstrap untuk layout dan komponen. Hindari mengubah file library langsung; perbarui dari sumber upstream.
`index.html` ditambahkan agar tidak muncul listing direktori.
