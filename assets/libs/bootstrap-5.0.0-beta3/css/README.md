# assets/libs/bootstrap-5.0.0-beta3/css/

Contains Bootstrap CSS files. Use the minified CSS for production.
`index.html` added to prevent directory listing.

Berisi file CSS Bootstrap. Gunakan file minified untuk production.
`index.html` ditambahkan untuk mencegah listing direktori.
