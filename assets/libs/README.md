# assets/libs/

Third-party libraries (Bootstrap, icons, etc.). Keep upstream directory structure intact to ease updates.
`index.html` present to prevent directory listing.

Library pihak ketiga (Bootstrap, icons, dll). Pertahankan struktur upstream agar mudah diupdate.
`index.html` hadir untuk mencegah listing direktori.
